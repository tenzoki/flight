# flight-workbench/ is a per-project runtime artifact created in user projects,
# not in this plugin source tree. If it ever appears here (e.g. during dev
# testing), it should not be committed.
flight-workbench/
fusion-workbench/

# Stray local fusion tooling copy that can land at the repo root during dev.
.fusion/

# Per-user activity log is a personal runtime artifact, not plugin source.
activity-log-*.md

# IDE / OS noise
.DS_Store
.idea/
.vscode/
*.swp
